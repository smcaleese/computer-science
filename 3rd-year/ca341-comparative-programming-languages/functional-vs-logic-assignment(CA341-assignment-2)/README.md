## How to Run the Functional Implementation

In the directory /functional-solution, run the following commands:

`ghci`

`:load main.hs`

`main`

## How to Run the Logic Implementation

In the directory /logic-solution, run the following commands:

`swipl`

`["BST"].`

`main.`