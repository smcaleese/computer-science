integer subtract(x:integer, y:integer)
{
    var result:integer;
    result = x - y;
    return (result);
}

integer print(x:integer)
{
    return (x);
}

main
{
    var answer:integer;
    const x:integer = 2;
    const y:integer = 1;
    answer = subtract(x, y);
    print(answer);
}
