integer add(x:integer, y:integer)
{
    y = y + 1;
    while(x < 10) {
        x = x + 1;
    }
    if(y < 0) {
        y = y + 1;
    } else {
        x = x + 2;
    }
    return (x);
}

main
{
    var x:integer;
    var y:integer;
    x = 1;
    y = 2;
    add(x, y);
}
